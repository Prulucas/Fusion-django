from django.test import TestCase

# Create your tests here.


def add_num(num):
    return num + 1


class SimpleTestCase(TestCase):

    # roda sempre
    def setUp(self):
        self.numero = 41
        print('Iniciando o TestCase')

    # Testa a unidade de código:
    def test_add_num(self):
        value = add_num(self.numero)
        self.assertTrue(value == 42)


# Todo teste, começa com "test_"
'''
    º Para executar o teste é preciso  estar no mesmo diretorio que o manage.py no terminal e rodar o comando
    'python manage.py test', se retornar 'Ok', Se falhar ele fala a linha que deu erro e o nome do teste
    
    º Para organizar melhor deve-se deletar esse arquivo tests e criar um diretório de 'tests' e colocar um 
arquivo python chamado '__init__.py' que transforma o diretório em um pacote python

    º Temos que saber o que precisamos testar em nosso projeto, temos algumas bibliotecas que failitam o 
nosso trabalho, como a 'model_mommy' 'coverage'

    º Após a instalação das bibliotecas, deve-se criar um arquivo na raiz do projeto chamado: '.coveragerc'
o arquivo de configuração do coverage

Aquivo 'coveragecr':
[run]
source = .

# e omitir:
omit = 
    */__init__.py
    */settings.py
    */manage.py
    */wsgi.py
    */apps.py
    */urls.py
    */admin
    */tests/*
    */migrations/*
------------------------

    isso mostra a raiz do projeto onde queremos testar para a biblioteca

    Rodar no terminal: 'coverage run manage.py test'

    Para fazer o relatório dele: 'coverage report' 
    Relatório html: 'coverage html' -> cria um arquivo html, ai para vizualizar a página: 'python -m http.server'
    Para iniciar o server: 'python -m http.server' (IPV6)
    'python -m http.server 8000 --bind 127.0.0.1' (IPV4)
'''
